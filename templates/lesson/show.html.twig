{% extends 'base.html.twig' %}

{% block title %}{{ lesson.name }} / {{ lesson.course.name }}  / StudyOn{% endblock %}

{% block body %}
    <h1>{{ lesson.name }}</h1>

    <div>
        Курс - <a href="{{ path('app_course_show', {'id': course.id}) }}">{{ course.name }}</a>
    </div>

    <p>
        <a href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">Редактировать</a>
        {{ include('lesson/_delete_form.html.twig') }}
    </p>

    <p>{{ lesson.content }}</p>

{% endblock %}
