{% extends 'base.html.twig' %}

{% block title %}{{ lesson.name }} / {{ lesson.course.name }}  / StudyOn{% endblock %}

{% block body %}
    <h1>{{ lesson.name }}</h1>

    <div class="fs-5">
        Курс <a style="text-decoration: none" href="{{ path('app_course_show', {'id': course.id}) }}">{{ course.name }}</a>
    </div>

    <div class="d-flex my-3">
        <a class="btn btn-outline-secondary me-1" role="button" href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">Редактировать</a>
        {{ include('lesson/_delete_form.html.twig') }}
    </div>

    <div>
        <p class="rounded border p-4">{{ lesson.content }}</p>
    </div>

{% endblock %}
