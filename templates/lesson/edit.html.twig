{% extends 'base.html.twig' %}

{% block title %} {{ lesson.name }} / {{ lesson.course.name }}  / StudyOn{% endblock %}

{% block body %}
    <h1>{{ lesson.name }}</h1>

    <div class="fs-5 mb-3">
        Курс <a style="text-decoration: none" href="{{ path('app_course_show', {'id': lesson.course.id}) }}">{{ lesson.course.name }}</a>
    </div>

    {{ include('lesson/_form.html.twig', {'button_label': 'Сохранить'}) }}

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
